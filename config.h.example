#ifndef INCLUDE_CONFIG_H
#define INCLUDE_CONFIG_H

#define ENABLE_SENSOR_SHT40

//	#define ENABLE_SENSOR_BME280
#if defined(ENABLE_SENSOR_BME280)
	//	#define SENSOR_BME280_I2C
	//	#define SENSOR_BME280_VSPI
#endif

//	#define ENABLE_SENSOR_PMS5003
#if defined(ENABLE_SENSOR_PMS5003)
	static uint8_t const SENSOR_PMS5003_RX = 19;
	static uint8_t const SENSOR_PMS5003_TX = 23;
	static uint8_t const SENSOR_PMS5003_SET = 04;
	static uint8_t const SENSOR_PMS5003_RESET = 25;
#endif

#define DEFAULT_SENSORS { \
	[0] = std::nullopt, \
	[SHT40] = 0, \
	[BME280] = std::nullopt, \
	[PMS5003] = std::nullopt \
}

static String campaign_name = "123";
static String organisation_name = "OrganisationName";
static String device_name = "234";
static String AP_SSID = "Provide my WiFi SSID";
static String AP_PASS = "Provide my WiFi password";
static String STA_SSID = "Use this WiFi SSID";
static String STA_PASS = "Use this WiFi password";
static String monitor_URL = "http://host1.example.com:80/path/to/monitor";
static String upload_URL = "http://host2.example.com:80/path/to/upload";
static String upload_username = "upload username";
static String upload_password = "upload password";
static unsigned int measure_interval = 60000;
static bool use_AP_mode = true;

/* calibrated = reading * slope + intercept */
static float calibration_slope = 1.0;
static float calibration_intercept = 0.0;

static signed int const timezone = 8 * 60; /* minutes */
static unsigned int const display_refresh_interval = 5; /* seconds */
static unsigned int const start_wait_time = 6000; /* milliseconds */
static unsigned int const reboot_wait_time = 6000; /* milliseconds */
static unsigned int const main_loop_delay = 1000; /* milliseconds */
static unsigned int const WiFi_wait_time = 20000; /* milliseconds */
static unsigned int const WiFi_check_interval = 10000; /* milliseconds */
static unsigned int const serial_baudrate = 115200; /* milliseconds */
static unsigned int const reinitialize_interval = 15000; /* milliseconds */
static unsigned char const reset_pin = 34;

static unsigned int const measure_interval_lowerbound = 15;
static unsigned int const measure_interval_upperbound = 900;
static float const calibration_slop_lowerbound = 0.75;
static float const calibration_slop_upperbound = 1.25;
static float const calibration_intercept_lowerbound = -10.0;
static float const calibration_intercept_upperbound = 10.0;

#endif /* INCLUDE_CONFIG_H */
